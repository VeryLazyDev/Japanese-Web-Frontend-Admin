import QuestionFilter from "./questions_filter";

const QuestionSection = () => {
    return (
        <section>
            <QuestionFilter />
        </section>
    );
};
export default QuestionSection;
